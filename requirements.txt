# PyPDF Toolbox GUI - Requirements
# Core PDF processing libraries
PyPDF2>=3.0.0
pypdf>=4.0.0
pymupdf>=1.23.0  # For PDF thumbnail generation and text extraction (no external dependencies)
pdf2image>=1.16.0  # Alternative for thumbnails (requires poppler)
reportlab>=4.0.0

# OCR support
pytesseract>=0.3.10
pillow>=10.0.0
ocrmypdf>=16.0.0  # For OCR-based text extraction from scanned PDFs

# GUI framework
# tkinter is included with Python standard library
tkinterdnd2>=0.3.0  # Drag and drop support

# Configuration
pyyaml>=6.0.0  # YAML configuration files

# HTTP requests (for Azure API calls)
requests>=2.31.0

# Azure AI (optional - for AI-powered tools)
openai>=1.0.0  # Azure OpenAI client
azure-identity>=1.15.0  # Azure Entra ID authentication (optional)

# Additional utilities
tqdm>=4.65.0

# Markdown converter support
markdown>=3.4.0  # Markdown parsing and conversion
python-docx>=1.1.0  # DOCX export support
html2text>=2024.2.26  # HTML to text conversion (optional)
tkinterweb>=3.0.0  # Embedded HTML browser widget for preview
# weasyprint>=60.0  # Alternative PDF generation (optional, has system dependencies)
